database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://spring-petclinic-postgres.cdxclukjp1hv.us-east-1.rds.amazonaws.com}
spring.datasource.username=${POSTGRES_USER:LuisGarrigosa}
spring.datasource.password=${POSTGRES_PASS:Admin.123}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
